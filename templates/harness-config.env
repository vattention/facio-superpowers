# .harness/config.env — team-shared environment variables sourced by harness skills
#
# ── 提交规则 ───────────────────────────────────────────────────
# 私有仓库: 可提交（团队共用 Lark webhook URL 等基线值）
# 公开仓库: 加入 .gitignore，改用 shell profile 或 CI secrets 注入
#
# ── 加载时机 ───────────────────────────────────────────────────
# spec-ratifier pre-check 在检查环境变量前自动 source 此文件。
# 文件中的赋值会覆盖 shell profile 中的同名变量（file-wins），
# 因此团队基线值优先于个人 export。如需临时覆盖，请注释对应行后重跑。
#
# ── 用法 ───────────────────────────────────────────────────────
# 1. 取消下方对应行注释
# 2. 填入实际值
# 3. 私有仓库 commit；公开仓库勿提交
#

# Required by spec-ratifier (Lark broadcast on ratification)
# 获取方式: 飞书群 → 群设置 → 群机器人 → 添加自定义机器人
# FACIO_LARK_WEBHOOK_URL=https://open.feishu.cn/open-apis/bot/v2/hook/xxx
